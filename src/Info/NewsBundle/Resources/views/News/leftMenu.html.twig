<div class="col-xs-3 leftMenu">

    <ul class="nav nav-pills nav-stacked">

        {% for page in entity %}

            <li role="presentation">
                <a href="{{ path('info_page_get', {'id':page.id}) }}">{{ page.title }}</a>
            </li>

        {% endfor %}


        <li role="presentation"><a href="{{ path('info_suggest_list') }}">Суроо-жоопторdsfvdsv {% fdf %}</a></li>
    </ul>
</div>
