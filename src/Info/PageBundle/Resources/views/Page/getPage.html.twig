{% extends '::base.html.twig' %}
{% block body %}
    <div class="container">
        <div class="row">
            {# left Menu start#}
            {{ render(controller('InfoPageBundle:Page:leftMenu')) }}
            {# left Menu end#}

            <div class="col-md-9">
                <h1 style="color:#2f2bff;  text-align: center" >{{ page.title }}<br> </h1>
                <h3>{{ page.text | raw }} </h3> <br>
                <br><br>
            </div>
            {# right menu start  #}
            {#{{ include('InfoPageBundle:Default:rightMenu.html.twig') }}#}
            {# right menu end #}
        </div>
    </div>

{% endblock %}